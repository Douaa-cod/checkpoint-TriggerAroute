import { ColorWordsDirective } from './color-words.directive';

describe('ColorWordsDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorWordsDirective();
    expect(directive).toBeTruthy();
  });
});
